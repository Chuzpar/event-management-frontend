function Hero() {
  return (
    <section className="hero">
      <h1>Discover Amazing Events Near You</h1>
      <p>
        Find concerts, conferences, workshops, and community events all in one place.
      </p>

      <button>Explore Events</button>
    </section>
  );
}

export default Hero;